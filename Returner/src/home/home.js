import React, { Component } from 'react';
import {
  Platform,
  StyleSheet,
  Text,
  ScrollView,
  View
} from 'react-native';

class Home extends Component {

  state = {
  }

  componentDidMount() {
  }

  render() {
    return (
      <View style={{flex: 1}}>
      </View>
    )
  }
}

export default Home;
