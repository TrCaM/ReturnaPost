import React, { Component } from 'react';
import {
  Platform,
  StyleSheet,
  Text,
  ScrollView,
  View
} from 'react-native';

class Shipments extends Component {

  state = {
  }

  componentDidMount() {
  }

  render() {
    return (
      <View style={{flex: 1}}>
        <ScrollView>
        </ScrollView>
      </View>
    )
  }
}

export default Shipments;
